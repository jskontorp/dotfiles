# macOS-specific tmux overrides — shared config loaded from ~/.tmux.shared.conf
source-file ~/.tmux.shared.conf

# Forward OSC 7 (cwd reporting) to the outer terminal so Ghostty
# can open new tabs in the same directory even when inside tmux
set -as terminal-features ",xterm-ghostty:osc7"
