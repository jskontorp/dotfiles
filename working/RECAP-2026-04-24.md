# Recap 2026-04-24 — Where we are and what's next

Single-session context, written so future-you (or a cold Claude session) can pick up without re-litigating decisions.

## Context

Migrating the agentic-coding setup from pi-only to shared pi + Claude Code. Skills, subagents, settings, and the bootstrap flow are in place. Remaining work is the Langfuse feedback pipeline + a few parked skills.

## Done (2026-04-22 → 2026-04-23)

- **Audit pass** on the full Claude config — 14 findings (3 high, 5 medium, 4 low) across CLAUDE.md leakage, skill compatibility, subagent glue, and bootstrap. All applied. Committed as `238d95f agents: tighten safety defaults across pi + claude`.
- **`install.sh` marketplace-skill gap fixed.** Previously marketplace skills only landed in `~/.pi/agent/skills/`; now they also mirror to `~/.claude/skills/` (global) and `<repo>/.claude/skills/` (project-scoped), honouring `claude-compatible: false`. Retroactively linked existing entries (systematic-debugging, webapp-testing, accessibility, etc.). Committed as `5cafdb9`.
- **Three superpowers skills cherry-picked** via `just add-skill`:
  - `verification-before-completion` (P0 — pre-completion adversarial check)
  - `subagent-driven-development` (P1 — controller + two-stage review pattern)
  - `finishing-a-development-branch` (P2 — post-implementation dispatcher, complements `prepare-merge`)
- **Two investigation markdowns** in `ideas/`:
  - `LANGFUSE_PLAN.md` — implementation plan with decisions confirmed
  - `PLANS_LOCATION_INVESTIGATION.md` — where writing-plans output should live, mem0 assessment
- **Verified `systematic-debugging`** auto-triggers correctly (no `disable-model-invocation`, description catches common "debug" phrasings).

## Decisions locked

Langfuse pipeline (baked into `ideas/LANGFUSE_PLAN.md`):

1. **Host:** existing `ssh oracle` VM (24 GB RAM, 121 GB free disk, Docker 29 + Compose v5 installed, aarch64 Ubuntu 22.04). P1 of the plan can be skipped during execution.
2. **Network:** Cloudflare Tunnel + Cloudflare Access (email OTP). No public IP.
3. **Feedback marker:** free-form `!<token>`. Any `!` followed by non-whitespace. Every marker becomes a Langfuse tag; full prompt goes into metadata for later LLM-parsed pattern mining. No numeric score at capture time.
4. **Sanitization:** none. Personal retrospective logging, self-hosted, behind Cloudflare Access. Do not use for shared or production data.
5. **Retention:** unlimited (no TTL).

## Parked — revisit when it matters

| Item | Why parked | Revisit trigger |
|---|---|---|
| `claude-md-management` plugin | Official Anthropic, safe with approval gate, not urgent | When CLAUDE.md feels stale or you want a periodic audit |
| `browser-inspect` custom skill | Build only with concrete use case | Next CSS/rendering "deploy-to-debug" loop |
| Fork `writing-plans` skill | Adoption blocked on plan-location decision | When you want to start using plan artefacts |
| mem0 | Wrong tool (runtime memory; we want retrospective) | If session-continuity memory ever becomes the bottleneck |
| `explanatory-output-style` | Permanent skip — fights NAJA/ELIND | N/A |

## Uncommitted right now

```
M ideas/LANGFUSE_PLAN.md    # decision-reflecting edits (host, marker, sanitization, retention)
M pi/skill-lock.json        # adds for subagent-driven-development + finishing-a-development-branch
?? tmp/                     # scratch, untracked, ignore
```

Commit first two. Suggested message:
```
feat(skills): add sdd + finishing-branch, bake langfuse decisions into plan
```

## Next actions, in order

### 1. Commit pending edits (2 min)
```
git add ideas/LANGFUSE_PLAN.md pi/skill-lock.json
git commit
```

### 2. LANGFUSE_PLAN.md — P0 prerequisites (30 min)
- Pick a Cloudflare-hosted subdomain for Langfuse (e.g. `langfuse.<your-zone>`).
- Generate three secrets, store in password manager:
  - `NEXTAUTH_SECRET=$(openssl rand -base64 32)`
  - `SALT=$(openssl rand -base64 32)`
  - `ENCRYPTION_KEY=$(openssl rand -hex 32)` — must be 64 hex chars
- `ssh oracle`; confirm `docker ps` works without sudo (check `docker` group membership).
- Install `cloudflared` on the Oracle VM: https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/

### 3. LANGFUSE_PLAN.md — P2 deploy (1-2 hrs)
- Pull Langfuse's official `docker-compose.yml` — link inside P2.
- Store compose + `.env.example` in dotfiles at `services/langfuse/` (new dir).
- Add `just langfuse-up` / `just langfuse-down` recipes (mirror the existing justfile style).
- Create Cloudflare Tunnel: subdomain → `localhost:3000` on Oracle; gate behind Cloudflare Access email OTP.
- Verify: browser loads Langfuse UI on the subdomain; restart `langfuse-*` containers, data persists.

### 4. LANGFUSE_PLAN.md — P4 Claude Code hooks (1-2 hrs)
- Port `doneyli/claude-code-langfuse-template` into `claude/hooks/langfuse_hook.py` (Stop hook). No sanitization pass needed — skip that section.
- Write `claude/hooks/outcome_tag.py` (UserPromptSubmit). Simpler than the plan's original spec because marker is free-form:
  - Regex `!(\S+)` across the prompt text, collect all matches.
  - Write `~/.claude/state/pending_outcome.json` with `{session_id, markers, raw_prompt, timestamp}`.
  - Langfuse Stop hook on next turn reads the file, applies `trace.update(tags=markers, metadata={...})`, deletes the state file.
- Extend `install.sh` to mirror `claude/hooks/*.py` into `~/.claude/hooks/`. Pattern: copy the agent-mirror block at `install.sh` lines 107-117, add `chmod +x` after linking.
- Add `Stop` + `UserPromptSubmit` hook stanzas + `env.TRACE_TO_LANGFUSE=true` to `claude/settings.json`.
- Smoke test: one throwaway session, `!test` in a prompt, verify the trace + tag land in Langfuse UI.

### 5. (optional) P5 — analysis workflow
- `scripts/langfuse-analyze.py` + `just langfuse-analyze [days=7]`.
- Not urgent. Build after ~20+ feedback-tagged traces exist.

## File map

| Artefact | Path |
|---|---|
| Langfuse implementation plan (source of truth) | `ideas/LANGFUSE_PLAN.md` |
| Plans-location investigation | `ideas/PLANS_LOCATION_INVESTIGATION.md` |
| This recap | `ideas/RECAP-2026-04-24.md` |
| Older research (reference) | `research-human-ai-coding-collaboration.md`, `CLAUDE_CODE_WISHLIST.md` |

## Cold-start cheat (for a future Claude session)

Read this file first, then `ideas/LANGFUSE_PLAN.md`. Skip the rest — they're archival. The two investigation markdowns already encode every decision reached; don't re-litigate.

## Session reference

Pick up from this Claude Code session transcript if needed:

`~/.claude/projects/-Users-jorgens-kontorp-code-personal-dotfiles/1c824c79-ac40-4dee-b5cb-5be6b53f0a94.jsonl`
