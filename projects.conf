# Project-specific pi config — candidate directories per project.
# Format: <project-name> <candidate-path> (one per line, multiple candidates per project OK)
# First candidate that contains a .git dir wins.

malevich     ~/code/personal/malevich
